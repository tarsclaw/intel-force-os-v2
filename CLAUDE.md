# CortexOS

**Loaded by Claude Code at the start of every session in this directory. Read it first.**

---

## What CortexOS is

> _One paragraph describing the product / project goes here. Replace this stub when you know what CortexOS is._

---

## Repository layout

```
CortexOS/
├── CLAUDE.md          ← You're reading this
├── .claude/
│   └── settings.json  ← Permissions, hooks, MCP servers (project-local)
├── docs/              ← Specs, ADRs, planning
└── src/               ← Code (when there is some)
```

---

## How Claude Code should work in this project

Default behaviour applies. Add project-specific guidance here as the project takes shape:
- Tech stack and conventions
- Commands (npm scripts, slash commands)
- Things never to do
- Where to ask for clarification vs. ship

---

## Current state

**Stage:** scaffold — not yet building anything.

When work begins, track build slices here in a small table. Same pattern as
sibling project `intel-force-os` (`/Users/madsadmin/code/intel-force-os/CLAUDE.md`)
if a richer template is useful.
