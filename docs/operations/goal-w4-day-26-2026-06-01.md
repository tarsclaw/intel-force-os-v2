# /goal — W4 Day-26 autonomous build (2026-06-01)

**Window:** ~10h. Phase 1 (compound, 1.5h) → Phase 2 (build, 7h) → Phase 3 stretch (1.5h). Founder fires Bullhorn chase + email verify in parallel; non-blocking.

## §0 Read first
CLAUDE.md (5 rules + 4 boundaries + Karpathy) · .agents/current-priorities.md header · docs/operations/goal-week-4-track-1.md §2+§9 · packages/mcp-connectors/{companies-house,xero}/ (pattern) · agents/recruitment/cash-conductor/agent.md §3+§4 (Phase 2 consumer).

## §1 Phase 1 — compounding (5 commits, ~1.5h)
1. Commit founder's incident report verbatim → `docs/incidents/2026-06-01-intelforce-ai-email-outage.md`
2. Refactor `.agents/current-priorities.md` → ≤120-line header (today + blockers + action board); extract Day-N-Shipped sections → `docs/operations/decision-log.md` (append-only). Cuts session-startup cost ~80%.
3. `CLAUDE.md` session-ritual addendum — start (git log -10 + status + priorities header + verify CTX_INSTANCE_ID=ifos-v2) + end (commit WIP + header update + tree-clean assertion).
4. `.agents/learnings/01-codex-roundtrip-discipline.md` — codify hard ≤2 round-trip ceiling; R3 = escalate via disagreement doc, never iterate.
5. ECC pattern extraction — read affaan-m/ECC "Token Optimization" + "Memory Persistence" guides; extract ≤3 IFOS-native patterns → `.agents/learnings/02-ecc-patterns-extracted.md`. NO ecc packages installed (Karpathy precedent — pattern adoption only).

## §2 Phase 2 — build (3 commits, ~7h)
6. `@ifos/quickbooks` MCP — mirror `@ifos/xero`; per-realm OAuth diff in README; ≥15 vitest (scaffold + rate-limit + auth/concurrent-dedup + capabilities); README ≥150 lines citing Intuit rate-limits verbatim; fixture-first.
7. `@ifos/open-banking` MCP — TrueLayer + Plaid UK via `config.provider`; 90-day PSD2 token-aging info/warn/blocking at ≤30/14/7d; property-based test day 0-100 boundary; capabilities: oauth_refresh, list_transactions_since, get_account_balance; ≥15 vitest; README ≥150.
8. Cluster F manifest entry in `scripts/run-codex-ratification.sh` — xero + quickbooks + open-banking via `mcp-connector` skill. Cash Conductor bundle queued for next manifest update when scaffold lands.

## §3 Phase 3 — stretch (~1.5h if Phase 2 done <19:00 local)
9. Cash Conductor bundle skeletons — `cycle.sh` (14-step per agent.md §4) + `validate.sh` (Gate A per §5) + `tools.yaml`. Fixtures + `context.sh` + `cleanup.sh` deferred. Drafts-only graceful degradation when `autosend-bridge-telegram` absent (D1-B doc).

## §4 Scope OUT
No commercial signups · no live API (fixture-first; `MCP_LIVE_TESTS` off) · no Codex runs (founder triggers F) · no live VPS · no edits to ratified artefacts outside scope · no ECC package installs · no cortextOS submodule edits except shadow points.

## §5 Per-commit gates
`pnpm typecheck` clean · `pnpm test` green (≥15 vitest per new MCP) · `shellcheck` clean · 0 hardcoded secrets · 0 Composio/AgentMail in agent.md/tools.yaml/fixtures · re-grep cited line numbers AFTER edit · atomic per-artefact commits + Co-Authored-By footer · tests BEFORE commit; never commit red.

## §6 Failure modes
Phase 1 >2h: commit, skip to Phase 2 · API ambiguity: commit clean fixtures, flag rest · red tests: STOP, fix · any artefact needing Codex R3: STOP, escalate via disagreement doc.

## §7 Ask vs proceed
Proceed: connector internals · fixtures · ECC pattern shape · README · per-step Codex remediation within ≤2.
Ask: deviation from agent.md contracts · commercial signup · live API call · ratified-artefact edit · ECC package install · Codex disagreement.

## §8 End-of-day report
Print: commits + SHAs · test counts per new package · Phase 1 ✓ + context-cost reduction · ECC patterns adopted (≤3) · Phase 2 status (qb / open-banking; partial OK) · Phase 3 reached? · founder action board refresh · tomorrow's first action: Cash Conductor bundle + cluster F run.

*End of /goal.*
