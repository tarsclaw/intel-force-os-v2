RATIFIED

Round-3 verdict: the invariant update is narrow and preserves T1-T12. T1 now explicitly distinguishes nullable `recent_edit.original_text` from non-null `tenant_slug`, and §6 tracks the future `autosend_approval_mappings` inventory/audit update as a bridge-implementation obligation.
