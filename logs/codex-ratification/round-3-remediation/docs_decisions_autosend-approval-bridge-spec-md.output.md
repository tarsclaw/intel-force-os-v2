RATIFIED

Round-3 verdict: the category mapping now uses the actual cortextOS `ApprovalCategory` enum (`external-comms`, `financial`, `deployment`, `data-deletion`, `other`) and the spec now names the future tenancy-invariant/audit inventory update for `autosend_approval_mappings` without pretending the table exists today.
