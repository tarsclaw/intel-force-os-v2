{
  "name": "@ifos/bullhorn",
  "version": "0.1.0",
  "private": true,
  "description": "IFOS Bullhorn MCP connector — two-step OAuth (access_token + BhRestToken) + REST candidate/placement/client/contact read + note write. Used by Janitor + Scribe + Concierge agents per agents/recruitment/{janitor,scribe,concierge}/tools.yaml. Direct API per-tenant OAuth path per docs/decisions/bullhorn-integration-path.md Sub-decision A RESOLVED 2026-06-02; marketplace deferred to v1.1+.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": { "node": ">=20.0.0" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
