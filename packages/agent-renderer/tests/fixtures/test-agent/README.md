# test-agent

Minimal test fixture bundle for `packages/agent-renderer/` unit + integration
tests. NOT a v1.0 production agent. Used only by `vitest` runs + the manual
acceptance command `pnpm render -- render test-agent --tenant migration-test`.

Six bundle files + three fixture sub-directories per master brief §8 layout.
