# ─── services/graphify-service ─────────────────────────────────────────────

# Anthropic — graphify's extraction step calls Claude
ANTHROPIC_API_KEY=sk-ant-...

# Optional override of the graphify CLI path (default: just `graphify`)
GRAPHIFY_BIN=graphify
